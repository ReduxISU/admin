# Intentionally no org-level settings. The ruleset lives in suborgs/all-repos.yml.
#
# Do not move it back here. A `rulesets:` block in THIS file is created as an *organization*
# ruleset — lib/settings.js hardcodes SCOPE.ORG for the org config and strips `rulesets` before
# cascading to repos. That path needs the App to hold org Administration: write, and org rulesets
# additionally require a Team/Enterprise plan. Rulesets in suborgs/*.yml and repos/*.yml are
# repo-scoped, which is what we want.
{}
